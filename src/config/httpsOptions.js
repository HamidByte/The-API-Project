const fs = require('fs')

const httpsOptions = {
  // key: fs.readFileSync('./.data/certificates/cert.key'),
  // cert: fs.readFileSync('./.data/certificates/cert.crt')
}

module.exports = httpsOptions
